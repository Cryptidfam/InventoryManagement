<Window x:Class="InventoryManagement.AddItems"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add New Item" Height="400" Width="600" SizeToContent="WidthAndHeight">
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.Resources>
            <Style TargetType="TextBox">
                <Setter Property="Width" Value="30"></Setter> 
            </Style>
        </Grid.Resources>
        <TextBlock Grid.Column="0" Grid.Row="0" Text="Name"></TextBlock>
        <TextBox Grid.Column="1" Grid.Row="0" Text="{Binding SelectedItem.Name, UpdateSourceTrigger=PropertyChanged}"></TextBox>

        <TextBlock Grid.Column="0" Grid.Row="1" Text="Category"></TextBlock>
        <TextBox Grid.Column="1" Grid.Row="1" Text="{Binding SelectedItem.Category, UpdateSourceTrigger=PropertyChanged}"></TextBox>

        <TextBlock Grid.Column="0" Grid.Row="2" Text="Quantity"></TextBlock>
        <TextBox Grid.Column="1" Grid.Row="2" Text="{Binding SelectedItem.Quantity, UpdateSourceTrigger=PropertyChanged}"></TextBox>

        <TextBlock Grid.Column="0" Grid.Row="3" Text="Price"></TextBlock>
        <TextBox Grid.Column="1" Grid.Row="3" Text="{Binding SelectedItem.Price, UpdateSourceTrigger=PropertyChanged}"></TextBox>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,10">
            <Button Content="Add Item" Width="100" Margin="5" Click="AddItem_Click"/>
        </StackPanel>

    </Grid>
</Window>
